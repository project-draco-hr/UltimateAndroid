{
  super.onCreate(savedInstanceState);
  setContentView(R.layout.motion_activity_parallax);
  if (savedInstanceState == null) {
    getFragmentManager().beginTransaction().add(R.id.container,new ParallaxFragment()).commit();
  }
}
