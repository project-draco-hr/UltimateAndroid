{
  post(new Runnable(){
    @Override public void run(){
      if (smoothScroll) {
        smoothScrollToPosition(selectedIndex);
      }
 else {
        setSelection(selectedIndex);
      }
    }
  }
);
}
