{
  String result="";
  if (judgeVerionOfSdk() > 7) {
    result=Base64.encodeToString(context,type);
  }
 else {
    result=com.fss.commons.commonUtils.urlUtils.Base64.encodeBytes(context);
  }
  return result;
}
