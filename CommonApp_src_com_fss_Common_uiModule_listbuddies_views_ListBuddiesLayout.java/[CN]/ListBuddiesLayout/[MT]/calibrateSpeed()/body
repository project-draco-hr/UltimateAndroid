{
  if (mSpeed == 1) {
    mSpeed=DEFAULT_SPEED;
  }
  if (isAutoScrollLeftListFaster) {
    mSpeedLeft=mSpeed;
    mSpeedRight=mSpeed / 2;
  }
 else {
    mSpeedLeft=mSpeed / 2;
    mSpeedRight=mSpeed;
  }
}
