{
  if (lastUpdated != null) {
    mHeaderUpdateTextView.setVisibility(View.GONE);
    mHeaderUpdateTextView.setText(lastUpdated);
  }
 else {
    mHeaderUpdateTextView.setVisibility(View.GONE);
  }
}
